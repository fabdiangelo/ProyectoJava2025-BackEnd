version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL
      - DB_PASSWORD
      - CORREO
      - APP_PASSWD_GMAIL
      - YOUTUBE_API_KEY
      - SECRET_KEY